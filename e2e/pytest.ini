# config do pytest pro e2e - precisa do backend e frontend rodando antes
[pytest]
testpaths = .
base_url = http://localhost:8000
# coleta arquivos que comecam com numero (01_, 02_...) alem do padrao test_
python_files = [0-9]*.py test_*.py
